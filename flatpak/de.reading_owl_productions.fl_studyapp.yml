# Generated from flatpak-flutter.yml, do not edit
# Visit the flatpak-flutter project at https://github.com/TheAppgineer/flatpak-flutter
id: de.reading_owl_productions.fl_studyapp
runtime: org.freedesktop.Platform
runtime-version: '24.08'
sdk: org.freedesktop.Sdk
sdk-extensions:
  - org.freedesktop.Sdk.Extension.llvm19
command: fl_studyapp
finish-args:
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  - --device=dri
modules:
  - name: fl_studyapp
    buildsystem: simple
    build-options:
      arch:
        x86_64:
          env:
            BUNDLE_PATH: build/linux/x64/release/bundle
        aarch64:
          env:
            BUNDLE_PATH: build/linux/arm64/release/bundle
      append-path: /usr/lib/sdk/llvm19/bin:/var/lib/flutter/bin
      prepend-ld-library-path: /usr/lib/sdk/llvm19/lib
      env:
        PUB_CACHE: /run/build/fl_studyapp/.pub-cache
    build-commands:
      - setup-flutter.sh -C .
      - flutter build linux --release
      - install -D $BUNDLE_PATH/fl_studyapp /app/bin/fl_studyapp
      - cp -r $BUNDLE_PATH/lib /app/bin/lib
      - cp -r $BUNDLE_PATH/data /app/bin/data
      - install -Dm644 assets/icons/icon.png /app/share/icons/hicolor/64x64/apps/de.reading_owl_productions.fl_studyapp.png
      - mv fl_studyapp.desktop de.reading_owl_productions.fl_studyapp.desktop
      - install -Dm644  de.reading_owl_productions.fl_studyapp.desktop -t "${FLATPAK_DEST}/share/applications" 

    sources:
      - pubspec-sources.json
      - type: git
        url: https://github.com/ReadingOwl3000/fl_studyapp.git
        commit: 28dec5c
    modules:
      - flutter-sdk-3.29.3.json
 
 
